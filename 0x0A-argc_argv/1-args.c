#include "main.h"
#include <stdio.h>

/**
 * main - the enrty point
 * @argc: argumrnt number
 * @argv: array argumrnt num
 * Return: the value to returned
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc - 1);
	return (0);
}
