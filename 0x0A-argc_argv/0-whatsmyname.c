#include <stdio.h>

/**
 * main - th  entry point
 * @argv: parameter contain array str for the name
 * @argc: to be for printing name
 * Return: the value to be returned
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
